# Handoff — 2026-07-10 — NeuroNiche Spaces phases 1–6 core

## Done (verified)
- Phase 1 schema + design doc — evidence: commit bffc0b6, `supabase/migrations/0001_init.sql`, `docs/SCHEMA-DESIGN.md`
- Phase 2 funding matcher engine (data-driven, non-AU = empty set) — evidence: commit fba8974, 5/5 tests `src/lib/funding/match.test.ts`
- Phase 2 researched AU funding seed DRAFT with official citations — evidence: commit 616d6f7, `supabase/seed_funding_au.sql` (NOT applied; awaiting Stefan review — 3 of 4 named grants may not accept direct school applications; NCCD has no per-student amount)
- Phases 3–4 planner UI + logic, checklist assistant, CSV export — evidence: commit ec983e7, `npm run build` green, 5/5 tests `src/lib/planner/plan.test.ts`
- Phase 6 integration tests AU vs non-AU pipeline — evidence: commit 136d393, 12/12 total pass (`node --test "src/lib/**/*.test.ts"`), tsc exit 0
- Phase 5 compliance audit: ZERO violations — evidence: audit section in `.planning/PHASES.md`

## Remaining
- Stefan reviews `supabase/seed_funding_au.sql` (approve/cut/add rows)
- Create Supabase project + run migration (click-steps given in session; then wire client + .env)
- Auth phase: RLS membership policies (org tables currently locked shut)
- Resend email reminders (stub in `src/lib/assistant.ts`; needs API key)
- PDF export (CSV shipped)
- Phase 6 visual review of layout diagrams — run `npm run dev`, screenshot `/`
- Vercel staging deploy (ASK STEFAN FIRST — deploy rule)
- Stripe flat-fee tiers (later phase; re-run compliance audit after)

## Files touched
- supabase/migrations/0001_init.sql — 5 tables, CHECKs enforce non-diagnostic categories, RLS on
- supabase/seed_funding_au.sql — DRAFT researched funding rows, cited
- src/lib/funding/match.ts(+test) — matcher engine
- src/lib/planner/plan.ts(+test) — shopping list + grid layout
- src/lib/assistant.ts — checklist, deadlineInfo, CSV
- src/lib/integration.test.ts — AU vs non-AU pipeline
- src/app/page.tsx — whole app UI (demo data inline until Supabase)
- docs/SCHEMA-DESIGN.md, .planning/PHASES.md — decisions of record

## Risks / open questions
- Demo product catalogue + funding constants in page.tsx MIRROR the seed file — keep in sync until Supabase wired
- Grant eligibility caveats (schools often need auspicing orgs) may reshape product pitch: "funding pathways" not "your dollar figure"
- Layout algorithm is greedy shelf-packing, no doors/windows (ponytail comment marks upgrade path)
- Tests run via `node --test "src/lib/**/*.test.ts"` (Node 24 native TS); `allowImportingTsExtensions` added to tsconfig for this

## Constraints reaffirmed
- No student-identifiable data; no diagnosis labels (DB-enforced)
- No commission/success-fee billing — flat tiers only
- Funding always cited + "not guaranteed" disclaimer
- Country split enforced at engine level, not just UI
- Calm UX: deadlines as plain dates, no urgency styling
