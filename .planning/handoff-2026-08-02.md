# Handoff — 2026-08-02 — Spatial Design Engine (7 phases + audit remediation)

## Done (verified)
- Phase 1 schema + shared Zustand state — evidence: `supabase/migrations/0002_sensory_7dim.sql`, `0003_room_layouts.sql`, `src/lib/spatial/{types,clearance,store}.ts`, 4/4 tests
- Phase 2 2D editor (walls/doors/snapping/clearance) — evidence: `src/components/spatial/{RoomEditor2D,WallLayer,ObjectLayer}.tsx`, `src/lib/spatial/geometry.ts`
- Phase 3 3D viewer (WebGPU+WebGL2 fallback, orbit, walk-through) — evidence: `src/components/spatial/{RoomViewer3D,RoomGeometry3D,ObjectMesh3D,WalkControls3D,webgpu}.tsx`
- Phase 4 properties panel — evidence: `src/components/spatial/PropertiesPanel.tsx`
- Phase 5 scenario templates (5 templates, AUD budget ranges) — evidence: `src/lib/spatial/templates.ts(+test)`
- Phase 6 export engine (CSV + browser-print PDF) — evidence: `src/lib/spatial/bom.ts(+test)`, `src/components/spatial/{ExportPanel,PrintableExport}.tsx`
- Phase 7 integration — all pieces wired live at `/spatial` (`src/app/spatial/page.tsx`)
- Full audit run (Fable5/Sonnet loop, 1 Opus escalation on WebGPU canvas-capture timing — resolved, no code change needed) — every finding fixed:
  - B1 (duplicate doors orphaned) — store.ts `addDoor` now replaces per-wall
  - B2 (size sliders no-op) — `updateObjectProps` now syncs `footprintM`
  - B3 (template load silently overwrote room dims) — only defaults on first-ever load
  - B4 (templates never carried `clearanceRadiusM`) — added to movement objects across 4 templates
  - B5 (walls drawable outside floor bounds) — `clampPointToBounds` in geometry.ts
  - S1 (4 npm audit highs) — `next` 16.2.10→16.2.12, `postcss`/`sharp` forced via `package.json` `overrides` (next's own vendored copies were stale; **did not** apply npm's suggested `next@9.3.3` downgrade, that was a resolver false-positive) — **0 vulnerabilities now**
  - S2 (CSV formula injection) — fixed in both `bom.ts` and `assistant.ts`'s `csvCell`
  - High-detail WebGPU render tier — built, feeds the PDF 3D snapshot
  - Printable export — now draws doors + objects, not just walls
  - Live catalogue data — extracted `src/app/page.tsx`'s `CATALOGUE`/`FUNDING` into `src/lib/demoData.ts`, shared by both pages (removes the two-fake-catalogues inconsistency)
  - Undo/redo — added to store.ts (50-entry bounded history)
  - Persistence — localStorage autosave + `BroadcastChannel` cross-tab sync (same-browser only, see Remaining)
  - Touch input — explicit touch handlers added to `RoomEditor2D`'s Konva Stage (Konva does not auto-normalize touch→mouse; verified in `node_modules/konva/lib/Stage.js`)
  - Reduced-motion — OS `prefers-reduced-motion` detection + manual toggle, damping disabled, walk-mode speed halved
- Follow-up remediation (4 UX/perf items missed in the first pass, caught when the user asked me to verify my own "all fixed" claim against the audit report line-by-line):
  - Keyboard alternative for canvas tools — Tab cycles object selection, arrow keys nudge position (Shift = 10x step)
  - First-run guidance — inline empty-state panel shown until a wall/template is added
  - Plain-language warning copy — "clearance violations" → "need more space around it/them"
  - Code-split the 3D viewer — `RoomViewer3D` now `next/dynamic` in both `page.tsx` and `ExportPanel.tsx` (both mount it); confirmed via build output showing three.js in separate ~650KB chunks, not the initial bundle
- Landing page (`/`) now links to the room designer (`/spatial`) — it was fully built and working but had zero discoverability path from the actual app entry point until this fix (commit a9a61fd)
- Final state: `tsc` clean, `eslint` clean, `node --test "src/lib/**/*.test.ts"` → 35/35 pass, `npm run build` succeeds, `npm audit` → 0 vulnerabilities

## Remaining (needs Stefan)
- **Live Supabase project + credentials** — migrations 0002/0003 are written but unapplied (same as 0001 always was). Persistence today is localStorage + same-browser cross-tab only; true cross-device sync and the `room_layouts`/`placed_objects` tables going live both need this.
- **Live `funding_sources` in exports** — same blocker; exports currently pull from `src/lib/demoData.ts` (shared, at least no longer duplicated/inconsistent)
- Visual/device QA never done — no browser available this session. Specifically unverified: WebGPU high-detail mode's actual visual difference, PDF snapshot capture in a real browser, touch drag on a real iPad, walk-mode comfort with reduced-motion on
- `npm warn allow-scripts`: `unrs-resolver` has a pending postinstall script not yet approved — informational, not a vulnerability; run `npm approve-scripts --allow-scripts-pending` to review if desired
- Multi-room support — persistence is single fixed-key MVP (`noniche-spatial-room-default`); needs a real room-id scheme once Supabase lands

## Files touched (this session, on top of prior sensory-taxonomy work)
- `supabase/migrations/0002_sensory_7dim.sql`, `0003_room_layouts.sql` — new tables, 7-dim taxonomy migration
- `src/lib/spatial/` — types, clearance, geometry, store, templates, bom (+tests for all)
- `src/components/spatial/` — full 2D/3D/properties/templates/export component tree
- `src/app/spatial/page.tsx` — integration shell
- `src/lib/demoData.ts` — extracted shared catalogue/funding demo data
- `src/lib/assistant.ts` — CSV injection fix only
- `package.json` — new deps (zustand, three, @react-three/fiber, @react-three/drei, konva, react-konva), next 16.2.12, postcss/sharp overrides

## Constraints reaffirmed
- No student-identifiable data; non-diagnostic sensory taxonomy (now 7-dim, DB-enforced)
- No new server-side PDF dependency — browser-native print, zero-dependency by design
- No destructive dependency downgrade taken without asking (declined npm's next@9.3.3 "fix")
- Calm UX maintained; reduced-motion added given this product's vestibular-sensitivity audience
