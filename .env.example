# Copy to .env.local and fill in real values. Never commit .env.local.

# Stripe (Phase 6 billing scaffold — see .planning/handoff-2026-08-11-stripe-scaffold.md)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Price ID for the flat-fee plan, created in the Stripe Dashboard (Product > Pricing).
STRIPE_PRICE_ID=
# Public base URL used to build Stripe success/cancel redirect links.
NEXT_PUBLIC_APP_URL=http://localhost:3000
